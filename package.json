{
  "private": true,
  "scripts": {
    "test": "kbb --backend sci --classpath src:test -e \"(require '[clojure.test :as t] 'ibm_ims.main-test) (let [r (t/run-tests 'ibm_ims.main-test)] (.exit js/process (if (pos? (+ (:fail r) (:error r))) 1 0)))\""
  }
}
